# Core dependencies for H5AD to Seurat Converter
# Python packages required for the conversion functionality

# Single-cell analysis
scanpy>=1.9.0
anndata>=0.8.0

# Data manipulation and analysis
pandas>=1.3.0
numpy>=1.20.0

# Optional but recommended for enhanced functionality
scipy>=1.7.0
h5py>=3.1.0

# For progress bars and user interface
tqdm>=4.60.0

# For command line argument parsing (built-in, but specified for clarity)
argparse

# For file path operations (built-in, but specified for clarity)
pathlib
